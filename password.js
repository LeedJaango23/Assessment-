let password = 'Jaango2323kjfkjghkjgkjhgl'
// let containsNumber = /[0-9]/.test(password)
// /\d/.test(password)

//  if(password.length >= 10 && /\d/.test(password) && /[a-z0-9]/.test(password)){
 if(password.length >= 10 && /\d/.test(password) && /[a-z0-9]/.test(password)){
     console.log('password good')

} else {
    console.log('password no good')
}